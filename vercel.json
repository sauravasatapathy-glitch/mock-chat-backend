{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "includeFiles": [
        "lib/**"
      ]
    }
  },
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.js" }
  ]
}
